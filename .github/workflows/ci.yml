name: CI Tests (Expected FAIL)

on:
  push:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: |
          pip install pytest selenium
      
      - name: Setup Chrome
        uses: browser-actions/setup-chrome@v1
      
      - name: Start server
        run: |
          if [ -d "public" ]; then cd public; fi
          python3 -m http.server 8000 &
          sleep 5
      
      - name: Run tests
        id: tests
        run: pytest tests/ -v
      
      - name: Create Bug Report
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `🐛 Bug Report: CI Tests - ${context.sha.substring(0,7)}`,
              labels: ['bug', 'ci'],
              body: `## 🐛 Баг-репорт

              **Результат тестов:** ${{ steps.tests.outcome }}

              ### Детали:
              - **Ветка:** ${context.ref}
              - **Коммит:** ${context.sha}
              - **Автор:** ${context.actor}
              - **Время:** ${new Date().toLocaleString()}

              ### Логи ошибок:
              \`\`\`
              Тесты завершились с результатом: ${{ steps.tests.outcome }}
              \`\`\`

              ### Ссылка на запуск:
              ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}

              ---
              *Автоматически созданный баг-репорт из CI/CD пайплайна*`
            });
